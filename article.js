document.addEventListener('DOMContentLoaded',async()=>{const id=new URLSearchParams(location.search).get('id'),view=document.getElementById('articleView');try{const c=await WINN.content(),a=WINN.articleById(c,id)||c.articles[0];document.title=`${a.title} | FINANCIEEL DAGBLAD WINN`;view.innerHTML=`<span class="article-section">${WINN.esc(a.section.toUpperCase())}</span><h1>${WINN.esc(a.title)}</h1><p class="article-subtitle">${WINN.esc(a.subtitle||'')}</p><div class="article-byline">FINANCIEEL DAGBLAD WINN · ${WINN.fmtDate(a.date)} · ${WINN.esc(c.settings.location||'Paramaribo')}</div><img class="article-image" src="${WINN.esc(a.image||'assets/favicon.png')}" alt=""><p class="article-lead">${WINN.esc(a.lead)}</p><div class="article-body">${(a.body||[]).map(p=>`<p>${WINN.esc(p)}</p>`).join('')}</div>`;document.getElementById('relatedArticles').innerHTML=c.articles.filter(x=>x.id!==a.id).slice(0,5).map(x=>`<a class="related-link" href="${WINN.articleLink(x)}"><span>${WINN.esc(x.section.toUpperCase())}</span><b>${WINN.esc(x.title)}</b></a>`).join('')}catch(e){view.innerHTML='<h1>Artikel niet gevonden</h1><p>Ga terug naar de voorpagina.</p>'}});